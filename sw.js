const CACHE="lab6-v21";
const ASSETS=["./","./index.html","./lab-engine.js?v=17","./manifest.webmanifest","./data.json"];
async function notify(type,percent,text){const clientsList=await self.clients.matchAll({includeUncontrolled:true,type:"window"});clientsList.forEach(c=>c.postMessage({type,percent,text}))}
self.addEventListener("install",event=>event.waitUntil((async()=>{const cache=await caches.open(CACHE);for(const asset of ASSETS){try{await cache.add(asset)}catch(e){console.warn("Cache asset",asset,e)}}await self.skipWaiting()})()));
self.addEventListener("activate",event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener("message",event=>{if(event.data&&event.data.type==="SKIP_WAITING")self.skipWaiting()});
self.addEventListener("fetch",event=>{const u=new URL(event.request.url);if(event.request.method!=="GET")return;if(u.pathname.endsWith("/data.json")){event.respondWith(fetch(event.request,{cache:"no-store"}).then(async r=>{const c=await caches.open(CACHE);c.put("./data.json",r.clone());return r}).catch(()=>caches.match("./data.json")));return}if(event.request.mode==="navigate"||u.pathname.endsWith("/index.html")){event.respondWith(fetch(event.request,{cache:"no-store"}).then(r=>{caches.open(CACHE).then(c=>c.put("./index.html",r.clone()));return r}).catch(()=>caches.match("./index.html")));return}event.respondWith(caches.match(event.request).then(c=>c||fetch(event.request)))});
